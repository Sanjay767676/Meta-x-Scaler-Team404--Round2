# Extra training stack — install with root requirements.txt (Spaces / Dockerfile.train).
# Root requirements.txt already includes torch/transformers/PEFT; this file adds overlap for Colab-only flows.

torch>=2.3.0
transformers>=4.41.0
trl>=0.9.4
peft>=0.11.1
datasets>=2.19.0
accelerate>=0.30.0
bitsandbytes>=0.43.1

# Unsloth: install in Colab per CUDA — https://github.com/unslothai/unsloth
